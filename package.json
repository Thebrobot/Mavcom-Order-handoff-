{
  "name": "mav-order-hand-off",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix deal-form",
    "build": "npm run build --prefix deal-form",
    "preview": "npm run preview --prefix deal-form"
  }
}
