# Deal submission form (Brobot)

All-in-one deal handoff UI (Vite + React). Root repo scripts use `--prefix deal-form`.

```bash
# from repository root
npm run dev
npm run build
```

Configure `deal-form/.env` from `.env.example` (`VITE_SUBMIT_WEBHOOK_URL`).
